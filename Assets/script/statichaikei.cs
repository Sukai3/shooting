using UnityEngine;

public class statichaikei : MonoBehaviour
{
    public static int kaisuu=0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        kaisuu = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
